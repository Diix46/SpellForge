# Keep the build context small + never ship local state or secrets.
node_modules
.output
.nuxt
.data
.git
.github
.vscode
.idea
*.log
.env
.env.*
agent-coach
.workflow-data
.eve
npm-debug.log*
.DS_Store
# The HD card generator has its own image (scripts/recompose/Dockerfile);
# its fonts are licensed and never shipped.
scripts/recompose
